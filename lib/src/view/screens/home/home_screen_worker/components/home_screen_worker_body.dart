import 'package:flutter/material.dart';

class HomeScreenWorkerBody extends StatelessWidget {
  const HomeScreenWorkerBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
