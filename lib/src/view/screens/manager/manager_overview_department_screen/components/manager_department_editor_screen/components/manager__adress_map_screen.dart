import 'package:flutter/material.dart';

class ManagerAdressMapScreen extends StatelessWidget {
  const ManagerAdressMapScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text("Adresse festlegen"),
      ),
      body: Center(
        child: Text("HELLO"),
      ),
    );
  }
}
